# Install

```sh
# run install script
./install.sh

# install : xmllint
dnf install xmllint

# for better usage also install
sd          # search and replace
snippets-ls # go snippets
tmux  
lazygit + delta
```
